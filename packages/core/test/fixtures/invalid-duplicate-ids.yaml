version: 1
name: invalid-duplicate-ids
agents:
  mock: { type: mock }
workflow:
  steps:
    - id: step_a
      run: program
      cmd: ["echo", "first"]
    - id: step_a
      run: program
      cmd: ["echo", "second"]
