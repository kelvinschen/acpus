import { defineWorkflow, z } from "acpus/core";

const value: string = 1;

export default defineWorkflow({
  name: "cli-type-error",
  inputSchema: z.object({ ready: z.boolean() }),
}).build(({ input }) => ({ ready: input.ready, value }));
