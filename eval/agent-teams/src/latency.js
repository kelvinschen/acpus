/**
 * Work item 2: summarize latency for a set of evaluation runs.
 *
 * @param {readonly { durationMs: number }[]} runs
 */
export function summarizeLatency(_runs) {
  throw new Error("TODO(work-item-2): implement summarizeLatency");
}
