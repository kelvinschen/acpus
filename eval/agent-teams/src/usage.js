/**
 * Work item 3: summarize token usage for a set of evaluation runs.
 *
 * @param {readonly {
 *   inputTokens?: number,
 *   outputTokens?: number,
 *   cacheReadTokens?: number
 * }[]} runs
 */
export function summarizeUsage(_runs) {
  throw new Error("TODO(work-item-3): implement summarizeUsage");
}
