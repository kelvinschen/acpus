/**
 * Work item 1: summarize outcome quality for a set of evaluation runs.
 *
 * @param {readonly { id: string, status: string }[]} runs
 */
export function summarizeQuality(_runs) {
  throw new Error("TODO(work-item-1): implement summarizeQuality");
}
