version: 1
name: invalid-duplicate-ids
agents:
  mock: { type: command, use: "echo" }
workflow:
  steps:
    - id: step_a
      run: program
      cmd: ["echo", "first"]
    - id: step_a
      run: program
      cmd: ["echo", "second"]
