# @acpus/core

`@acpus/core` contains the Acpus Workflow Spec compiler boundary: YAML parsing, schema validation, expression collection, diagnostics, frozen IR generation, and schedule projection.

Most users should install the `acpus` CLI package.

See the repository README for full usage: https://github.com/kelvinschen/acpus
