---
"@acpus/workflow-compiler": patch
---

Remove the unused named `rules` export from the repository-internal ESLint
plugin subpath while preserving the default plugin export and its
`acpus-internal/check` rule.
