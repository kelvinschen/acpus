---
"@acpus/runtime": minor
---

Update the acpx dependency to 0.12.0 and default `ACPX_CLAUDE_INCLUDE_USER_SETTINGS=1` for builtin `claude` agents; the inherited process environment and agent `env` can override the default.
