---
"@acpus/workflow-compiler": patch
"acpus": patch
---

Preserve physical package files when checking workflows installed outside the
workspace, and use workspace dependencies only when source-side resolution is
missing.
