---
"acpus": patch
---

Replace the bundled approval example with a request-only specify-approve-deliver workflow that confirms a natural-language contract, keeps implementation and repair in one Agent session, and publishes only an admitted, reviewed change.
