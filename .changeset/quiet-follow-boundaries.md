---
"acpus": minor
---

Remove the ineffective `workflow run --interval` option and define follow as a
read-only wait for the selected Runtime decision boundary rather than periodic
refresh or heartbeat.
