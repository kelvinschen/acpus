---
"acpus": minor
---

Remove the workflow init commands and bundled starter. Author workflows directly from the closest bundled example, where import comments expose every public runtime authoring helper, then validate with `acpus workflow check`.
