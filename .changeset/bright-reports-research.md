---
"acpus": patch
---

Add the bundled deep-research library workflow with configurable depth, language, and optional report rendering, separate reusable workflows from teaching examples, and recognize `/wf:` or `/workflow:` hints for library and catalog reuse.
