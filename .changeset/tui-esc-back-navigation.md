---
"@acpus/tui": minor
---

Add `Esc` key to navigate back from App (graph view) to RunPicker, allowing users to select a different run without quitting and relaunching the TUI. Footer now shows `Esc back` hint.
