version: 1
name: e2e-basic-run
workflow:
  steps:
    - id: build
      run: program
      cmd: "printf '{\"ok\":true}'"
      capture:
        stdout: true
        parse: json
      output:
        ok: boolean
outputs:
  ok: "${{ steps.build.output.ok }}"
